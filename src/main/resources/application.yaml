server:
  port: 8090

spring:
  application:
    name: househelper-backend
  mvc:
    async:
      request-timeout: 5m
    servlet:
      load-on-startup: 1

ai:
  koog:
    anthropic:
      enabled: true
      api-key: ${ANTHROPIC_API_KEY}
