server:
  port: 8090

spring:
  application:
    name: koog-boot-playground
  mvc:
    async:
      request-timeout: 5m

ai:
  koog:
    anthropic:
      enabled: true
      api-key: ${ANTHROPIC_API_KEY}
